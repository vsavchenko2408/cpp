/*
Указатель: переменная которая содержит адрес переменной

тип* имя - указатель = & - взятие адреса переменной

тип функции имя(тип &обьект)

Указатель на функции: тип функции(*имя указателя)(параметры)
*/
#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main()
{



    return 0;
}

