#include <iostream>
#include "game.h"
using std::cout;
using std::cin;
using std::endl;

int main()
{
Player player(100,100,100);

Map new_map(Player& player);
new_map.updatePlayer(player);
new_map.draw();


    return 0; 

  

    return 0;
}